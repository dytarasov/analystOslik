Ты — архитектор аналитических хранилищ. Тебе дана карта уже профилированного источника ClickHouse: таблицы (с доменом, грануляр­ностью, ключевыми колонками) и обнаруженные связи между ними. Нужно синтезировать бизнес-модель источника, чтобы агент-аналитик понимал смысл данных и канонические пути соединения.

Таблицы:
{% for t in tables %}
- `{{ t.qname }}` — {{ t.title or '—' }} (домен: {{ t.domain or '—' }}{% if t.grain %}, грануляр­ность: {{ t.grain }}{% endif %}{% if t.total_rows is not none %}, rows≈{{ t.total_rows }}{% endif %})
  ключевые колонки: {% for c in t.columns %}{{ c.name }}({{ c.role or '?' }}{% if c.key %}, key{% endif %}){% if not loop.last %}, {% endif %}{% endfor %}
{% endfor %}

Обнаруженные связи (from → to):
{% for e in edges %}- `{{ e.from }}` → `{{ e.to }}`{% if e.cardinality %} [{{ e.cardinality }}]{% endif %}{% if e.confidence is not none %} (conf={{ '%.2f'|format(e.confidence) }}){% endif %}
{% endfor %}
{% if glossary %}
Глоссарий источника от человека (авторитетно — используй ИМЕННО эти термины/определения/метрики, не противоречь им и не дублируй):
{{ glossary }}
{% endif %}
Задача:
1. Определи какие таблицы — фактовые (события/метрики), а какие — измерения (справочники), и опиши star/snowflake-структуру.
2. Опиши канонические пути джойнов (через какие колонки соединять факты с измерениями).
3. Предложи 5–15 предопределённых метрик — типовые агрегаты, которые аналитик будет считать по этим данным. Используй РЕАЛЬНЫЕ имена таблиц/колонок из карты выше. Выражение — корректный ClickHouse-фрагмент (то, что идёт после SELECT, напр. `count()`, `uniqExact(teacher_id)`, `sum(amount)`).
4. Сформируй глоссарий 5–20 бизнес-терминов с определениями и синонимами (рус/англ), чтобы текстовые запросы пользователя мапились на канонические сущности и колонки.

Верни строго один JSON-объект (без markdown, без пояснений):
```
{
  "overview_md": "Markdown-обзор источника: что это за данные, star-schema, фактовые vs измерения, канонические пути джойнов. 1-2 экрана.",
  "metrics": [
    {"name": "Активные учителя", "expression": "uniqExact(teacher_id)", "unit": "чел.", "description": "Кол-во уникальных учителей с активностью"}
  ],
  "glossary": [
    {"term": "Активный учитель", "definition": "Учитель, у которого была активность за период", "synonyms": ["active teacher", "активность"]}
  ]
}
```

Правила:
- Только существующие таблицы/колонки из карты. Не выдумывай.
- Метрики — осмысленные и переиспользуемые, а не тривиальные `count()` без контекста.
- Термины глоссария — те, что встретятся в запросах пользователя на естественном языке.
